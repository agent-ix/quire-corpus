id: inherent-implementation
family: types
language: rust
kind: control
issue_ref: agent-ix/quire-corpus#1
control_for: trait-implementation
comment: >-
  The same two declarations and the same method name, with the `for Persist`
  removed. An inherent impl implements nothing, so the correct output has no
  `implements_trait` edge at all. A producer that emits one because a trait
  of that shape exists in the file passes the positive and invents a
  relationship here.
